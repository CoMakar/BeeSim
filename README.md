# BeeSim

~ A project to model a simple bee hive.

```shell
# enter the shell
$ pipenv shell

# install dependencies
$ pipenv install

# build
$ pyinstaller main.spec

# | or |

# run
$ python main.py
```